{
  "name": "video-tools",
  "version": "0.0.5",
  "description": "",
  "main": "src/main/index.js",
  "workspaces": [
    "packages/*"
  ],
  "private": "true",
  "scripts": {
    "build": "lerna run build --stream",
    "pack": "lerna run pack --stream",
    "publish": "lerna run publish",
    "clean": "lerna run clean",
    "test": "lerna run test",
    "autoinstall": "sudo /usr/sbin/installer -store -pkg ./packages/videotools/electron-dist/videotools-0.0.5.pkg -target /"
  },
  "author": "chend0316",
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
    "asar": "^3.1.0",
    "dotenv": "^16.0.0",
    "ts-node": "^10.8.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/chend0316/videoinfo.git"
  }
}
